#!/bin/sh

VERSION="2.0"
UCIM=../uchome
NAME=nextim_uc_$VERSION

rm -rf webim $NAME

echo "ucim dir is " $UCIM
cp -R ../uchome webim 

cd webim 
make clean && make
rm -rf static_src* Makefile
cd ..

mkdir $NAME
mv webim $NAME/
zip -r $NAME.zip $NAME
rm -rf webim $NAME
